# dependencies
/node_modules
/.pnp
.pnp.*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# env files. Catch-all, because this repo is public: any .env variant is
# ignored unless it is explicitly an .example template.
.env
.env*
!.env*.example

# testing
/coverage

# prisma
/prisma/*.db

# local file storage (STORAGE_PROVIDER=local)
/storage

# editor
.vscode/*
!.vscode/extensions.json
.idea

# typescript incremental build cache
*.tsbuildinfo

# launch-video capture (see PROJECT.md §5l)
.env.capture
/storage-capture
/launch-video-assets
/capture/assets
